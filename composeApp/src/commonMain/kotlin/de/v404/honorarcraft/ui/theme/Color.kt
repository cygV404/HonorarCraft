package de.v404.honorarcraft.ui.theme

import androidx.compose.ui.graphics.Color

// Material-Vorlagenfarben. Sie stammen unverändert aus dem Compose-Projektgerüst und
// greifen nur dort, wo Android keine dynamischen Farben liefert (also immer auf dem Desktop).
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

/**
 * Markenfarben aus `res/values/colors.xml` der Android-App.
 *
 * Sie stecken bisher **nur** im Launcher-Icon — das Compose-Theme benutzt sie nicht. Hier
 * stehen sie, damit sie auf beiden Plattformen verfügbar sind, falls das Farbschema später
 * darauf aufgebaut werden soll.
 */
val BrandCyan = Color(0xFF18FFFF)
val BrandGreen = Color(0xFF00E676)

/** Hintergrund des Android-Startbildschirms, hell wie dunkel. */
val SplashBackground = Color(0xFF000000)
